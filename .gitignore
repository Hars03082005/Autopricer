# ── Node / Frontend ───────────────────────────────────────────────────────────
node_modules/

# Environment variables (contains secrets — never commit!)
.env
.env.local
.env.*.local

# Build output (generated; run `npm run build` to reproduce)
dist/
dist-ssr/
build/
*.local

# ── Python / Backend ──────────────────────────────────────────────────────────
venv/
.venv/
env/
ENV/

__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
pip-wheel-metadata/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*

# ── ML runtime / training cache ───────────────────────────────────────────────
catboost_info/
mlruns/
.wandb/

# ── Datasets (NEVER COMMIT DATASETS) ─────────────────────────────────────────
*.csv
*.xlsx
*.parquet
*.zip
data/
ml_training/data/

# ── ML training scripts: keep ONLY Variant 2 (clean_data-1.py & train-1.py) ──

ml_training/clean-1.py
ml_training/clean-2.py
ml_training/clean-3.py
ml_training/clean-s5.py



ml_training/train-1.py
ml_training/train-2.py
ml_training/train-3.py
ml_training/train-s5.py


# ── Model Registry: keep Variant 1, 2, and 3 ──────────────────────────────────
model_registry/variant_4/

# ── Large model artifacts ─────────────────────────────────────────────────────
model_artifacts/ensemble_*.pkl
model_artifacts/vehicle_price_catboost.cbm
model_artifacts/vehicle_price_xgboost.json
model_artifacts/training_report.json
model_artifacts_cell7/
model_artifacts_owner_assumed/
model_artifacts_split_compare/

# ── Mobile (Flutter) ──────────────────────────────────────────────────────────
mobile/.dart_tool/
mobile/.idea/
mobile/.flutter-plugins-dependencies
mobile/.metadata
mobile/build/
mobile/assets/flutter_assets/

# ── Environment & secrets ─────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
credentials.json
secrets.json

# ── Logs ──────────────────────────────────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── OS & editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── IDE / local tooling ───────────────────────────────────────────────────────
.gemini/
.cursor/
.agents/

# ── Jupyter Notebooks & checkpoints ───────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb

# ── Temp / scratch files ──────────────────────────────────────────────────────
ml_training/scratch_diff.txt
catboost_info/
ml_training/catboost_info/
analyze_raw.py
split_dataset.py
ml_training/find_dropped.py
